'use strict';

var assert = require('assert');
var nitrapiNode = require('../lib');
var accessToken = require('./accessToken.js');

describe('nitrapi-node', function () {
  it('should start up', function(done) {
    done();
  });
});
